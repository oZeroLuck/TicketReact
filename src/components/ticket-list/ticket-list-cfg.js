import {AddToCartBtn} from "../custom-button/btn-cfg";

/* TODO: ticket config: W.I.P.

Dependency: custom-button and it's cfgs.

The configurations works as follows:
    - buttons: [] -> Array of buttons, must be declared like this even if it's one;
    -

 */

const TicketWindow = {
    buttons: [AddToCartBtn]
}
