class LoginInfo {
    constructor(userInfo) {
        this.id = userInfo.id
        this.email = userInfo.email
        this.password = userInfo.password
        this.userId = userInfo.id
    }
}

export {LoginInfo}
